bin/
obj/
dist/
dist-host/
*.log
*.user
.vs/
tools/ImportCheck/bin/
tools/ImportCheck/obj/
install-maui.cmd
publish-host.cmd
_to_delete/
*.lnk
*.tgz

# Velopack: промежуточная папка публикации и готовые релизы
/build-vpk/
/Releases/
installer-build.log
vpk-install.log
vpk-add.log

# MSIX: промежуточная папка, готовые пакеты и добытые инструменты SDK
/build-msix/
/Store/
tools/msix/sdk/
/build-extension/
/demo-vault/
/dist-android/

# Ключ подписи Android — НИКОГДА не коммитить
/_signing/

# Секрет OAuth Google (локальный оверрайд рядом с приложением) — не коммитить.
# Секрет больше не хранится в исходниках; кладётся сюда или в переменную IPASSWRD_GOOGLE_SECRET.
google_oauth.json
**/google_oauth.json

/test-vault/

# Local backups, scratch and third-party tools - not for the repo
dist_backup_*/
dist_prev_*/
tools/platform-tools/
_gitlock_trash/
_trash/
